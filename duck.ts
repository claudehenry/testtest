/**
 * @description Define the purpose of the `quack` property.
 * It is a method that can be called on instances of the class.
 */
export class Duck {
  /**
   * @description Initializes an instance of the `Duck` class, allowing for object
   * creation without specifying properties. It does not define any specific behavior
   * or attributes, implying that they will be set elsewhere in the class or through inheritance.
   */
  constructor() {}

  
  /**
   * @description Defines an anonymous function with no arguments, which returns
   * undefined. This function is assigned to the `quack` property of the `Duck` class.
   */
  quack = () => {}
  
}
