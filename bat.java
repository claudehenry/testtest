/**
 * Contains a single method, B(), which is declared without any parameters.
 */
class A {
  /**
   * Does nothing and has no effect on the program's behavior. It is a null function.
   */
  void B() {}
}
